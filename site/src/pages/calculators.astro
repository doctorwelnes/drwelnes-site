---
import Header from "../components/Header.astro";
---

<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700;800&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/global.css" />
    <title>Калькуляторы</title>
    <style>
      :root {
        --bg1: radial-gradient(1200px 600px at 20% -10%, #dbeafe 0%, rgba(219,234,254,0) 55%),
          radial-gradient(900px 520px at 90% 0%, #fce7f3 0%, rgba(252,231,243,0) 55%),
          #ffffff;
        --ink: #111827;
        --muted: #4b5563;
        --stroke: rgba(229,231,235,.9);
        --surface: rgba(255,255,255,.8);
        --surface-strong: rgba(255,255,255,.85);
        --shadow: 0 10px 30px rgba(15,23,42,.08);
        --shadow-soft: 0 10px 30px rgba(15,23,42,.06);
      }

      html,
      body {
        margin: 0;
        padding: 0;
        overflow-x: hidden;
      }

      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      body {
        color: var(--ink);
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      }

      .page {
        min-height: 100vh;
        background: var(--bg1);
      }

      .container {
        max-width: 980px;
        margin: 0 auto;
        padding: 28px 20px 36px;
      }

      .header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        margin-bottom: 18px;
      }

      .brand {
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .brand-logo {
        display: block;
        width: 34px;
        height: 34px;
        border-radius: 10px;
        object-fit: cover;
      }

      .brand-name {
        font-weight: 700;
        letter-spacing: -0.01em;
      }

      .nav {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
      }

      .nav-link {
        text-decoration: none;
        color: var(--ink);
        padding: 8px 10px;
        border-radius: 10px;
      }

      .card {
        border: 1px solid var(--stroke);
        background: var(--surface);
        backdrop-filter: blur(10px);
        border-radius: 18px;
        box-shadow: var(--shadow-soft);
        padding: 16px;
      }

      .card--hero {
        padding: 22px;
        box-shadow: var(--shadow);
      }

      .hero-title {
        margin: 0 0 10px;
        font-size: 28px;
        line-height: 1.15;
        letter-spacing: -0.03em;
      }

      .hero-subtitle {
        margin: 0;
        color: var(--muted);
        font-size: 16px;
        line-height: 1.55;
        max-width: 64ch;
      }

      .tabs {
        margin-top: 12px;
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
      }

      .tab {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
        padding: 10px 14px;
        border-radius: 12px;
        text-decoration: none;
        background: #ffffff;
        color: var(--ink);
        border: 1px solid #e5e7eb;
        font-weight: 600;
      }

      .tab.is-active {
        background: var(--ink);
        color: #ffffff;
        border-color: var(--ink);
      }

      .panel {
        margin-top: 12px;
      }

      .panel.panel--first {
        margin-top: 16px;
      }

      .panel-title {
        margin: 0 0 12px;
        font-size: 18px;
      }

      .panel-hint {
        color: var(--muted);
        line-height: 1.5;
      }

      .form {
        display: grid;
        gap: 8px;
      }

      .form--420 {
        max-width: 420px;
      }

      .form--520 {
        max-width: 520px;
      }

      .form--560 {
        max-width: 560px;
      }

      .form--720 {
        max-width: 720px;
      }

      .grid {
        display: grid;
        gap: 8px;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      }

      .input {
        width: 100%;
        padding: 10px;
        border: 1px solid #e5e7eb;
        border-radius: 10px;
        font: inherit;
      }

      .btn {
        padding: 10px 12px;
        border-radius: 10px;
        border: 1px solid var(--ink);
        background: var(--ink);
        color: #ffffff;
        font-weight: 600;
        cursor: pointer;
      }

      .btn.btn--narrow {
        max-width: 220px;
      }

      .output {
        color: var(--ink);
      }

      .output-stack {
        margin-top: 6px;
        display: grid;
        gap: 8px;
      }

      .output-list {
        display: grid;
        gap: 6px;
      }

      .card-grid {
        margin-top: 16px;
        display: grid;
        gap: 12px;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      }

      @media (max-width: 420px) {
        .card-grid {
          grid-template-columns: 1fr;
        }
      }

      .calc-card {
        display: block;
        text-align: left;
        width: 100%;
        cursor: pointer;
        text-decoration: none;
        color: inherit;
      }

      .calc-card:hover {
        transform: translateY(-1px);
        transition: transform 160ms ease;
      }

      .calc-card-title {
        font-weight: 800;
        letter-spacing: -0.01em;
        font-size: 18px;
      }

      .calc-card-text {
        margin-top: 6px;
        color: var(--muted);
        line-height: 1.55;
        font-size: 16px;
      }

      .is-hidden {
        display: none;
      }
    </style>
  </head>
  <body>
    <main class="page">
      <div class="container">
        <Header />

        <section class="card card--hero">
          <h1 class="hero-title">Калькуляторы</h1>
          <p class="hero-subtitle">ИМТ, основной обмен энергии, разовое повторение и другое для Ваших целей</p>
        </section>

        <section class="card-grid" aria-label="Калькуляторы">
          <a href="/calculators/bmi" class="card calc-card">
            <div class="calc-card-title">ИМТ</div>
            <div class="calc-card-text">Узнайте за 30 секунд вашу идеальную зону веса и риски для здоровья!</div>
          </a>
          <a href="/calculators/tdee" class="card calc-card">
            <div class="calc-card-title">Основной обмен энергии</div>
            <div class="calc-card-text">Рассчитайте свой точный калораж за минуту — перестаньте гадать с едой!</div>
          </a>
          <a href="/calculators/protein" class="card calc-card">
            <div class="calc-card-title">Расчет нормы белка</div>
            <div class="calc-card-text">Быстро узнайте свой диапазон нормы белка (1,2–1,6 г/кг) на день.</div>
          </a>
          <a href="/calculators/1rm" class="card calc-card">
            <div class="calc-card-title">Разовое повторение (1RM)</div>
            <div class="calc-card-text">Раскройте свой силовой потолок мгновенно — какой вес Вам покорится?</div>
          </a>
          <a href="/calculators/hr-zones" class="card calc-card">
            <div class="calc-card-title">Пульсовые зоны</div>
            <div class="calc-card-text">Найдите свои идеальные пульсовые зоны — тренируйтесь умнее, безопаснее и результативнее!</div>
          </a>
        </section>

      </div>
    </main>
  </body>
</html>
