---
import Header from "../../components/Header.astro";
---

<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700;800&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/global.css" />
    <title>Пульсовые зоны</title>
    <style>
      :root {
        --bg1: radial-gradient(1200px 600px at 20% -10%, #dbeafe 0%, rgba(219,234,254,0) 55%),
          radial-gradient(900px 520px at 90% 0%, #fce7f3 0%, rgba(252,231,243,0) 55%),
          #ffffff;
        --ink: #111827;
        --muted: #4b5563;
        --stroke: rgba(229,231,235,.9);
        --surface: rgba(255,255,255,.8);
        --surface-strong: rgba(255,255,255,.85);
        --shadow: 0 10px 30px rgba(15,23,42,.08);
        --shadow-soft: 0 10px 30px rgba(15,23,42,.06);
      }

      html,
      body {
        margin: 0;
        padding: 0;
        overflow-x: hidden;
      }

      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      body {
        color: var(--ink);
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      }

      .page {
        min-height: 100vh;
        background: var(--bg1);
      }

      .container {
        max-width: 980px;
        margin: 0 auto;
        padding: 28px 20px 36px;
      }

      .header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        margin-bottom: 18px;
      }

      .brand {
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .brand-logo {
        display: block;
        width: 34px;
        height: 34px;
        border-radius: 10px;
        object-fit: cover;
      }

      .brand-name {
        font-weight: 700;
        letter-spacing: -0.01em;
      }

      .nav {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
      }

      .nav-link {
        text-decoration: none;
        color: var(--ink);
        padding: 8px 10px;
        border-radius: 10px;
      }

      .card {
        border: 1px solid var(--stroke);
        background: var(--surface);
        backdrop-filter: blur(10px);
        border-radius: 18px;
        box-shadow: var(--shadow-soft);
        padding: 16px;
      }

      .card--hero {
        padding: 22px;
        box-shadow: var(--shadow);
      }

      .hero-title {
        margin: 0 0 10px;
        font-size: 28px;
        line-height: 1.15;
        letter-spacing: -0.03em;
      }

      .hero-subtitle {
        margin: 0;
        color: var(--muted);
        font-size: 16px;
        line-height: 1.55;
        max-width: 64ch;
      }

      .panel {
        margin-top: 12px;
      }

      .panel-title {
        margin: 0 0 12px;
        font-size: 18px;
      }

      .form {
        display: grid;
        gap: 8px;
      }

      .form--560 {
        max-width: 560px;
      }

      .input {
        width: 100%;
        max-width: 100%;
        padding: 10px;
        border: 1px solid #e5e7eb;
        border-radius: 10px;
        font: inherit;
      }

      .btn {
        padding: 10px 12px;
        border-radius: 10px;
        border: 1px solid var(--ink);
        background: var(--ink);
        color: #ffffff;
        font-weight: 600;
        cursor: pointer;
      }

      .btn.btn--narrow {
        max-width: 220px;
      }

      .output {
        color: var(--ink);
        line-height: 1.55;
      }

      .output-stack {
        margin-top: 6px;
        display: grid;
        gap: 8px;
      }

      .output-list {
        display: grid;
        gap: 10px;
      }
    </style>
  </head>
  <body>
    <main class="page">
      <div class="container">
        <Header />

        <section class="card card--hero">
          <h1 class="hero-title">Пульсовые зоны</h1>
          <p class="hero-subtitle">Рассчитайте зоны по ЧССmax, чтобы тренироваться умнее и безопаснее.</p>
        </section>

        <section class="card panel">
          <h2 class="panel-title">Калькулятор</h2>
          <div class="form form--560">
            <label>Возраст (лет)<br /><input id="hr_age" type="number" class="input" /></label>
            <button id="hr_btn" type="button" class="btn btn--narrow">Посчитать зоны</button>
            <div id="hr_out" class="output"></div>
          </div>
        </section>

        <script>
          (function () {
            function n(v) {
              var x = Number(v);
              return Number.isFinite(x) ? x : NaN;
            }

            var hrBtn = document.getElementById('hr_btn');
            hrBtn && hrBtn.addEventListener('click', function () {
              var age = n(document.getElementById('hr_age').value);
              var out = document.getElementById('hr_out');
              if (!out) return;

              if (!age) { out.textContent = 'Введи возраст.'; return; }
              var hrMax = 220 - age;

              if (hrMax < 100 || hrMax > 240) { out.textContent = 'Проверь возраст: значение ЧССmax выглядит необычно.'; return; }

              function zone(p1, p2) {
                var a = Math.round(hrMax * p1);
                var b = Math.round(hrMax * p2);
                return a + '–' + b;
              }

              out.innerHTML =
                '<div class="output-stack">' +
                '<div><b>Ваша максимальная частота сердечных сокращений (ЧССmax):</b> ' + Math.round(hrMax) + ' уд/мин</div>' +
                '<div class="output-list">' +
                '<div><b>Зона 1</b> (50–60% от максимальной ЧСС): ' + zone(0.5, 0.6) + '<br />Умеренный пульс для здоровья и мягкого восстановления: укрепляет сердце, подготавливает организм к нагрузкам и подходит новичкам и ЛФК.</div>' +
                '<div><b>Зона 2</b> (60–70% от максимальной ЧСС): ' + zone(0.6, 0.7) + '<br />Комфортный «рабочий» пульс для сжигания жира и развития общей выносливости: организм активно использует жиры как топливо, дыхание остаётся относительно лёгким.</div>' +
                '<div><b>Зона 3</b> (70–80% от максимальной ЧСС): ' + zone(0.7, 0.8) + '<br />Аэробная тренировка в полную силу: растёт выносливость, плотность капиллярной сети и митохондрий, повышается общая спортивная форма.</div>' +
                '<div><b>Зона 4</b> (80–90% от максимальной ЧСС): ' + zone(0.8, 0.9) + '<br />Высокая интенсивность у анаэробного порога: развивает скорость, мощность и способность терпеть высокую нагрузку, используется для интервалов и подготовленных спортсменов.</div>' +
                '<div><b>Зона 5</b> (90–100% от максимальной ЧСС): ' + zone(0.9, 1.0) + '<br />Короткая работа на пределе возможностей: максимальная спринтерская скорость, рост VO2max и «финишная» мощность, доступна только в виде коротких отрезков из‑за резкого утомления.</div>' +
                '</div>' +
                '</div>';
            });
          })();
        </script>
      </div>
    </main>
  </body>
</html>
