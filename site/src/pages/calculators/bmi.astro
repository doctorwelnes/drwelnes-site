---
import Header from "../../components/Header.astro";
---

<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700;800&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/global.css" />
    <title>ИМТ</title>
    <style>
      :root {
        --bg1: radial-gradient(1200px 600px at 20% -10%, #dbeafe 0%, rgba(219,234,254,0) 55%),
          radial-gradient(900px 520px at 90% 0%, #fce7f3 0%, rgba(252,231,243,0) 55%),
          #ffffff;
        --ink: #111827;
        --muted: #4b5563;
        --stroke: rgba(229,231,235,.9);
        --surface: rgba(255,255,255,.8);
        --surface-strong: rgba(255,255,255,.85);
        --shadow: 0 10px 30px rgba(15,23,42,.08);
        --shadow-soft: 0 10px 30px rgba(15,23,42,.06);
      }

      html,
      body {
        margin: 0;
        padding: 0;
        overflow-x: hidden;
      }

      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      body {
        color: var(--ink);
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      }

      .page {
        min-height: 100vh;
        background: var(--bg1);
      }

      .container {
        max-width: 980px;
        margin: 0 auto;
        padding: 28px 20px 36px;
      }

      .header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        margin-bottom: 18px;
      }

      .brand {
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .brand-logo {
        display: block;
        width: 34px;
        height: 34px;
        border-radius: 10px;
        object-fit: cover;
      }

      .brand-name {
        font-weight: 700;
        letter-spacing: -0.01em;
      }

      .nav {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
      }

      .nav-link {
        text-decoration: none;
        color: var(--ink);
        padding: 8px 10px;
        border-radius: 10px;
      }

      .card {
        border: 1px solid var(--stroke);
        background: var(--surface);
        backdrop-filter: blur(10px);
        border-radius: 18px;
        box-shadow: var(--shadow-soft);
        padding: 16px;
      }

      .card--hero {
        padding: 22px;
        box-shadow: var(--shadow);
      }

      .hero-title {
        margin: 0 0 10px;
        font-size: 28px;
        line-height: 1.15;
        letter-spacing: -0.03em;
      }

      .hero-subtitle {
        margin: 0;
        color: var(--muted);
        font-size: 16px;
        line-height: 1.55;
        max-width: 64ch;
      }

      .panel {
        margin-top: 12px;
      }

      .panel-title {
        margin: 0 0 12px;
        font-size: 18px;
      }

      .form {
        display: grid;
        gap: 8px;
      }

      .form--420 {
        max-width: 420px;
      }

      .input {
        width: 100%;
        max-width: 100%;
        padding: 10px;
        border: 1px solid #e5e7eb;
        border-radius: 10px;
        font: inherit;
      }

      .btn {
        padding: 10px 12px;
        border-radius: 10px;
        border: 1px solid var(--ink);
        background: var(--ink);
        color: #ffffff;
        font-weight: 600;
        cursor: pointer;
      }

      .btn.btn--narrow {
        max-width: 220px;
      }

      .output {
        color: var(--ink);
        line-height: 1.55;
      }
    </style>
  </head>
  <body>
    <main class="page">
      <div class="container">
        <Header />

        <section class="card card--hero">
          <h1 class="hero-title">ИМТ</h1>
          <p class="hero-subtitle">Узнайте за 30 секунд вашу идеальную зону веса и риски для здоровья!</p>
        </section>

        <section class="card panel">
          <h2 class="panel-title">Калькулятор ИМТ</h2>
          <div class="form form--420">
            <label>Рост (см)<br /><input id="bmi_h" type="number" inputmode="decimal" class="input" /></label>
            <label>Вес (кг)<br /><input id="bmi_w" type="number" inputmode="decimal" class="input" /></label>
            <button id="bmi_btn" type="button" class="btn btn--narrow">Посчитать</button>
            <div id="bmi_out" class="output"></div>
          </div>
        </section>

        <script>
          (function () {
            function n(v) {
              var x = Number(v);
              return Number.isFinite(x) ? x : NaN;
            }

            var bmiBtn = document.getElementById('bmi_btn');
            bmiBtn && bmiBtn.addEventListener('click', function () {
              var h = n(document.getElementById('bmi_h').value) / 100;
              var w = n(document.getElementById('bmi_w').value);
              var out = document.getElementById('bmi_out');
              if (!out) return;
              if (!h || !w) { out.textContent = 'Заполни рост и вес.'; return; }

              var bmi = w / (h * h);
              var title = '';
              var msg = '';

              if (bmi < 18.5) {
                title = 'Дефицит';
                msg = 'Мнимая легкость в теле за счет сниженной массы тела, но усиливайте рацион — защититесь от анемии, остеопороза и инфекций, наберите мышцы для выносливости.';
              } else if (bmi < 25) {
                title = 'Норма';
                msg = 'Идеальный баланс — энергия бьёт ключом, сердце и суставы в гармонии, риски болезней на нуле. Здесь вы король фитнеса и повседневных побед!';
              } else if (bmi < 30) {
                title = 'Избыток';
                msg = 'Резерв энергии помогает, но корректируйте — предотвратите развитие диабета 2 типа, повышенного давления и нарушений метаболизма по типу повышенного холестерина. Лёгкий сдвиг откроет дверь к идеальной форме!';
              } else if (bmi < 35) {
                title = 'Ожирение 1';
                msg = 'Умеренные риски инфарктов (на 50% выше от стандартных рисков), артрита и хронической усталости — сброс 5-10% массы тела вернёт лёгкость, улучшит метаболизм и психологическую уверенность.';
              } else if (bmi < 40) {
                title = 'Ожирение 2';
                msg = 'Высокие риски диабета, апноэ сна и гипертонии (в 3 раза чаще от стандартных рисков) — план действий (диета + спорт) сократит угрозы вдвое и вернёт свободу движений и жизни.';
              } else {
                title = 'Ожирение 3';
                msg = 'Критические риски (инфаркты в 4 раза, рак, подвижность под угрозой от стандартных рисков) — даже 10% потери веса сотворит чудеса: Вы получите неимоверные плюсы для Вашего здоровья!';
              }

              out.innerHTML = '<b>ИМТ:</b> ' + bmi.toFixed(1) + '<br /><b>' + title + ':</b> ' + msg;
            });
          })();
        </script>
      </div>
    </main>
  </body>
</html>
